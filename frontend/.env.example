# Browser-visible values must use the NEXT_PUBLIC_ prefix. This is the API base
# URL that frontend code will use once Phase 3 introduces authenticated calls.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
